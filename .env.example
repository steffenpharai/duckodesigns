# ============================================
# Ducko Designs - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control

# ============================================
# Database Configuration
# ============================================
# PostgreSQL connection string
# For local development: postgresql://user:password@localhost:5432/duckodesigns
# For Cloud Run: Uses Secret Manager (cloud-run-database-url)
DATABASE_URL=postgresql://user:password@localhost:5432/duckodesigns

# ============================================
# NextAuth.js Configuration
# ============================================
# Secret key for NextAuth.js session encryption
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# Public URL of your application
# For local development: http://localhost:3000
# For production: https://duckodesigns-33ybtimjra-uc.a.run.app
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Google OAuth Configuration
# ============================================
# Required for authentication (Google OAuth only)
# Get from: https://console.cloud.google.com/apis/credentials?project=ducko-designs

# Google OAuth Client ID (not sensitive, can be public)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (SENSITIVE - keep secret!)
# Store in Secret Manager for production: google-client-secret
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# Google Cloud Storage Configuration
# ============================================
# Cloud Storage bucket name for storing order images and product images
GCS_BUCKET_NAME=duckodesigns-order-images

# Your Google Cloud Project ID
GCS_PROJECT_ID=ducko-designs

# Optional: Path to service account key file for local development
# Only needed if running locally and not using Application Default Credentials
# Leave empty if using: gcloud auth application-default login
GCS_KEY_FILE=

# ============================================
# Notes
# ============================================
# 1. For local development:
#    - Set DATABASE_URL to your local PostgreSQL instance
#    - Set NEXTAUTH_URL to http://localhost:3000
#    - Set GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET from Google Cloud Console
#    - Add http://localhost:3000/api/auth/callback/google to authorized redirect URIs
#
# 2. For Cloud Run deployment:
#    - DATABASE_URL is loaded from Secret Manager (cloud-run-database-url)
#    - NEXTAUTH_SECRET is loaded from Secret Manager (nextauth-secret)
#    - GOOGLE_CLIENT_SECRET is loaded from Secret Manager (google-client-secret)
#    - GOOGLE_CLIENT_ID should be set as environment variable
#    - NEXTAUTH_URL is automatically set by Cloud Build
#
# 3. Security:
#    - Never commit .env.local to version control
#    - Use Secret Manager for all sensitive values in production
#    - Rotate secrets regularly